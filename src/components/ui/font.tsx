import { Inter, Russo_One, Mulish } from 'next/font/google'

export const inter = Inter({ subsets: ['latin'] });
export const russo_one = Russo_One({ subsets: ['latin'], weight: ['400'] });
export const mulish = Mulish({ subsets: ['latin'], weight: ['400'] });